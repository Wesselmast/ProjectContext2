using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI; // Required when Using UI elements.

public class Scrollbar : MonoBehaviour
{
    public void Awake()
    {
        //transform.GetComponent<Scrollbar>().size = 0.076f;
    }
}
